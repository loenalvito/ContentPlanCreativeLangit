@extends('layouts.app') @section('content')<h1 class="text-2xl font-bold">My Tasks</h1><p class="text-xs text-slate-500 mb-5">Semua konten yang menjadi tanggung jawab saya</p>@foreach($groups as $label=>$items)<section class="mb-5"><h2 class="text-sm font-bold mb-2 {{ $label==='Overdue'?'text-red-600':'' }}">{{ $label }} ({{ $items->count() }})</h2><div class="panel divide-y">@forelse($items as $c)<a href="{{ route('content.show',$c) }}" class="flex justify-between p-4 text-xs hover:bg-slate-50"><span><b>{{ $c->title }}</b><br><span class="text-slate-400">{{ $c->platforms->pluck('name')->join(', ') }}</span></span><span>{{ $c->publish_date?->format('d M Y') }} · <span class="badge bg-blue-50 text-blue-600">{{ $c->status->label() }}</span></span></a>@empty<p class="p-4 text-xs text-slate-400">Tidak ada task.</p>@endforelse</div></section>@endforeach @endsection
