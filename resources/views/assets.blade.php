@extends('layouts.app') @section('content')<div x-data="{open:false}"><div class="flex justify-between mb-5"><div><h1 class="text-2xl font-bold">Assets Library</h1><p class="text-xs text-slate-500">Kumpulan aset dan template penting</p></div>@can('assets.create')<button @click="open=true" class="btn btn-blue">+ Add Asset</button>@endcan</div><div class="panel overflow-x-auto"><table class="table w-full"><thead><tr><th>Asset Name</th><th>Category</th><th>Type</th><th>Added By</th><th>Created</th><th>Link</th></tr></thead><tbody>@forelse($assets as $x)<tr><td class="font-semibold">{{ $x->title }}</td><td>{{ $x->category }}</td><td>{{ $x->asset_type }}</td><td>{{ $x->addedBy->name }}</td><td>{{ $x->created_at->format('d M Y') }}</td><td><a target="_blank" class="text-blue-600" href="{{ $x->url }}">↗</a></td></tr>@empty<tr><td colspan="6" class="text-center text-slate-400">Belum ada asset.</td></tr>@endforelse</tbody></table></div><div x-cloak x-show="open" class="fixed inset-0 bg-black/30 z-50 grid place-items-center"><form method="post" class="bg-white rounded-xl p-6 w-full max-w-md">@csrf<h2 class="font-bold mb-4">Add Asset</h2><input class="field w-full mb-3" name="title" placeholder="Asset name" required><select class="field w-full mb-3" name="category">@foreach(['Brand Asset','Template','Footage','Product','Event','Other'] as $x)<option>{{ $x }}</option>@endforeach</select><input class="field w-full mb-3" name="asset_type" placeholder="Type (Figma, Video, Image)"><input class="field w-full" type="url" name="url" placeholder="https://..." required><button class="btn btn-blue mt-4">Save Asset</button></form></div></div>@endsection
