@extends('layouts.app') @section('content')<div x-data="{open:false}"><div class="flex justify-between mb-5"><div><h1 class="text-2xl font-bold">Users</h1><p class="text-xs text-slate-500">Kelola akun, department, role, dan status</p></div>@can('users.create')<button @click="open=true" class="btn btn-blue">+ Add User</button>@endcan</div><div class="panel overflow-x-auto"><table class="table w-full"><thead><tr><th>Name</th><th>Email</th><th>Department</th><th>Role</th><th>Status</th><th>Last Login</th><th></th></tr></thead><tbody>@foreach($users as $u)<tr><td class="font-semibold">{{ $u->name }}</td><td>{{ $u->email }}</td><td>{{ $u->department?->name }}</td><td>{{ $u->roles->pluck('name')->join(', ') }}</td><td><span class="badge {{ $u->is_active?'bg-emerald-50 text-emerald-600':'bg-red-50 text-red-600' }}">{{ $u->is_active?'Active':'Inactive' }}</span></td><td>{{ $u->last_login_at?->diffForHumans() ?? 'Never' }}</td><td>@can('users.deactivate')<form method="post" action="{{ route('users.toggle',$u) }}" onsubmit="return confirm('Ubah status user ini?')">@csrf @method('PATCH')<button class="text-xs text-blue-600">Toggle</button></form>@endcan</td></tr>@endforeach</tbody></table></div><div x-cloak x-show="open" class="fixed inset-0 bg-black/30 z-50 grid place-items-center"><form method="post" action="{{ route('users.store') }}" class="bg-white rounded-xl p-6 w-full max-w-lg">@csrf<h2 class="font-bold mb-4">Add User</h2><div class="grid gap-3"><input class="field" name="name" placeholder="Name" required><input class="field" type="email" name="email" placeholder="Email" required><input class="field" type="password" name="password" placeholder="Password" required><select class="field" name="department_id">@foreach($departments as $x)<option value="{{ $x->id }}">{{ $x->name }}</option>@endforeach</select><select class="field" name="role">@foreach($roles as $x)<option>{{ $x->name }}</option>@endforeach</select></div><button class="btn btn-blue mt-4">Create User</button></form></div></div>@endsection
