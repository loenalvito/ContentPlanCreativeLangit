@extends('layouts.app')
@section('content')
@php
    $seriesData = $pillars->flatMap(fn ($pillar) => $pillar->series->map(fn ($series) => ['id' => $series->id, 'name' => $series->name, 'pillar_id' => $pillar->id]))->values();
    $selectedPlatforms = $content->platforms->pluck('id')->map(fn ($id) => (string) $id)->values();
    $selectedAccounts = $content->platforms->mapWithKeys(fn ($platform) => [(string) $platform->id => (string) $platform->pivot->account_id]);
@endphp
<div x-data="{editOpen:false,publishedOpen:{{ session('open_published_modal') ? 'true' : 'false' }},pillar:'{{ $content->pillar_id }}',series:@js($seriesData),selectedPlatforms:@js($selectedPlatforms),accountMap:@js($selectedAccounts),seriesList(){return this.series.filter(item=>String(item.pillar_id)===String(this.pillar))},togglePlatform(id){id=String(id);this.selectedPlatforms.includes(id)?this.selectedPlatforms=this.selectedPlatforms.filter(value=>value!==id):this.selectedPlatforms.push(id)}}" @keydown.escape.window="editOpen=false;publishedOpen=false">
    <a href="{{ route('content.index') }}" class="text-xs text-slate-500">← Content Plan</a>
    <div class="mt-4 mb-5 flex flex-wrap items-start justify-between gap-3">
        <div><h1 class="text-2xl font-bold">{{ $content->title }}</h1><span data-testid="content-detail-status" class="badge {{ $content->status === App\Enums\ContentStatus::Published ? 'bg-emerald-100 text-emerald-700' : 'bg-blue-50 text-blue-600' }}">{{ $content->status->label() }}</span></div>
        <div class="flex flex-wrap gap-2">
            @if($canEdit)<button data-testid="edit-content" type="button" @click="editOpen=true" class="btn border">Edit</button>@endif
            @if($content->status !== App\Enums\ContentStatus::Published && $canPublish)
                <form method="post" action="{{ route('content.publish', $content) }}">@csrf<button data-testid="publish-content" class="btn bg-emerald-600 text-white hover:bg-emerald-700">✓ Published</button></form>
            @elseif($content->status === App\Enums\ContentStatus::Published)
                <span class="btn cursor-default bg-emerald-50 text-emerald-700">✓ Published</span>
            @endif
        </div>
    </div>

    <div class="grid gap-4 lg:grid-cols-3">
        <section class="panel p-5 lg:col-span-2" x-data="{tab:'brief'}">
            <div class="flex gap-4 border-b mb-5 overflow-auto">@foreach(['brief'=>'Brief','script'=>'Script / Copy','assets'=>'Assets','comments'=>'Comments','revision'=>'Revision','activity'=>'Activity','source'=>'Source'] as $key=>$label)<button type="button" data-testid="tab-{{ $key }}" @click="tab='{{ $key }}'" class="flex items-center gap-2 pb-3 text-xs">{{ $label }} @if($key==='comments')<x-comment-indicator :total="$content->comments_count" :open="$content->open_comments_count" />@endif</button>@endforeach</div>

            <form x-show="tab==='brief'" method="post" action="{{ route('content.brief',$content) }}" class="grid gap-3">@csrf @method('PUT')@foreach(['hook'=>'Hook','angle'=>'Angle','key_message'=>'Key Message','cta'=>'CTA','notes'=>'Notes'] as $key=>$label)<label class="text-xs font-semibold">{{ $label }}<textarea class="field w-full" name="{{ $key }}" @disabled(!$canEdit)>{{ $content->brief?->$key }}</textarea></label>@endforeach @if($canEdit)<button class="btn btn-blue">Save Brief</button>@endif</form>
            <form x-show="tab==='script'" method="post" action="{{ route('content.brief',$content) }}" class="grid gap-3">@csrf @method('PUT')@foreach(['main_copy'=>'Main Script / Copy','reels_script'=>'Reels Script','carousel_copy'=>'Carousel Copy','caption'=>'Caption','threads_copy'=>'Threads Copy'] as $key=>$label)<label class="text-xs font-semibold">{{ $label }}<textarea class="field w-full h-28" name="{{ $key }}" @disabled(!$canEdit)>{{ $content->brief?->$key }}</textarea></label>@endforeach @if($canEdit)<button class="btn btn-blue">Save Script / Copy</button>@endif</form>
            <div x-show="tab==='assets'">@forelse($content->assets as $asset)<a href="{{ $asset->url }}" target="_blank" rel="noopener" class="block border rounded p-3 mb-2"><b>{{ $asset->title }}</b><div class="text-xs">{{ $asset->asset_type }} · {{ $asset->addedBy->name }} · {{ $asset->created_at->format('d M Y H:i') }}</div><p class="text-xs">{{ $asset->notes }}</p></a>@empty<p class="text-sm text-slate-400">No asset references yet.</p>@endforelse @if($canEdit)<form method="post" action="{{ route('content.assets',$content) }}" class="grid gap-2 mt-4">@csrf<input class="field" name="title" placeholder="Asset Name" required><select class="field" name="asset_type">@foreach(['Figma','Google Drive','Raw Footage','Reference','Image','Video','Thumbnail','Other'] as $type)<option>{{ $type }}</option>@endforeach</select><input class="field" type="url" name="url" placeholder="External URL" required><textarea class="field" name="notes" placeholder="Notes"></textarea><button class="btn btn-blue">Add Asset Reference</button></form>@endif</div>
            <div x-show="tab==='comments'">@forelse($content->comments as $comment)<div data-testid="comment" data-comment-status="{{ $comment->status }}" class="mb-2 rounded-xl border p-3 {{ $comment->status === 'resolved' ? 'border-emerald-100 bg-emerald-50/40 text-slate-500' : 'border-slate-200 bg-white' }}"><div class="flex items-start justify-between gap-3"><div><b>{{ $comment->user->name }}</b><span class="ml-2 text-xs text-slate-400">{{ $comment->created_at->format('d M Y H:i') }}</span></div>@if($comment->status==='resolved')<div class="text-right"><span class="badge bg-emerald-100 text-emerald-700">Resolved</span><div data-testid="resolved-meta" class="mt-1 text-[10px] text-slate-400">Resolved by {{ $comment->resolver?->name }} · {{ $comment->resolved_at?->format('d M Y H:i') }}</div></div>@else @can('comments.resolve')<form method="post" action="{{ route('content.comments.resolve',[$content,$comment]) }}">@csrf @method('PATCH')<button data-testid="resolve-comment" class="btn border">Resolve</button></form>@endcan @endif</div><p class="mt-2 text-sm">{{ $comment->body }}</p></div>@empty<p class="text-sm text-slate-400">No comments yet.</p>@endforelse @can('comments.create')<form method="post" action="{{ route('content.comments',$content) }}" class="mt-4">@csrf<textarea data-testid="comment-body" class="field w-full" name="body" required></textarea><button data-testid="comment-submit" class="btn btn-blue mt-2">Add Comment</button></form>@endcan</div>
            <div x-show="tab==='revision'">@foreach($content->revisions as $revision)<div class="border-b py-3 text-sm">{{ $revision->note }}<div class="text-xs text-slate-400">{{ $revision->author->name }} · {{ $revision->created_at->format('d M Y H:i') }}</div></div>@endforeach @if($content->status===App\Enums\ContentStatus::Review && auth()->user()->can('production.change_status'))<form method="post" action="{{ route('content.revision',$content) }}" class="mt-4">@csrf<textarea class="field w-full" name="body" placeholder="Revision note"></textarea><button class="btn bg-amber-500 text-white mt-2">Request Revision</button></form>@endif</div>
            <div x-show="tab==='activity'">@forelse($activities as $activity)<div class="border-l-2 pl-3 py-2 text-sm">{{ $activity->description }}<div class="text-xs">{{ $activity->created_at->diffForHumans() }}</div></div>@empty<p class="text-sm text-slate-400">No activity yet.</p>@endforelse</div>
            <div data-testid="source-panel" x-show="tab==='source'">@if($content->sourceIdea)<div class="bg-slate-50 p-4 text-sm"><b>Submitted By:</b> {{ $content->sourceIdea->submitter->name }}<br><b>Department:</b> {{ $content->sourceIdea->department->name }}<br><b>Original Idea:</b> {{ strip_tags($content->sourceIdea->idea) }}<br><b>Converted At:</b> {{ $content->created_at->format('d M Y H:i') }}</div>@else<p class="text-sm">Source: Direct Content<br><b>Created By:</b> {{ $content->creator?->name }}</p>@endif</div>
        </section>

        <aside class="space-y-4">
            <section class="panel p-5"><h2 class="font-bold mb-3">Content Metadata</h2>@foreach(['Publish Date'=>$content->publish_date?->format('d M Y'),'Account'=>$content->account?->display_name,'Platform'=>$content->platforms->pluck('name')->join(', '),'Pillar'=>$content->pillar->name,'Series'=>$content->series->name,'Format'=>$content->format?->name,'PIC'=>$content->pic?->name] as $label=>$value)<div class="flex justify-between gap-3 py-3 border-b text-xs"><span>{{ $label }}</span><b class="text-right">{{ $value?:'—' }}</b></div>@endforeach</section>
            @if($content->status === App\Enums\ContentStatus::Published)
                <section data-testid="published-information" class="panel p-5"><div class="flex items-center justify-between gap-2"><h2 class="font-bold">Published Information</h2>@if($canManagePublishedInformation)<button data-testid="edit-published-information" type="button" @click="publishedOpen=true" class="text-xs font-semibold text-blue-600">Edit</button>@endif</div><div class="mt-3 text-xs"><p><b>Status:</b> Published</p><p class="mt-2"><b>Posting Link:</b> @if($content->is_not_for_public)<span class="text-slate-500">Not for Public</span>@elseif($content->final_url)<a href="{{ $content->final_url }}" target="_blank" rel="noopener" class="break-all text-blue-600">{{ $content->final_url }}</a>@else<span class="text-amber-600">Not recorded yet</span>@endif</p><p class="mt-2"><b>Visibility:</b> {{ $content->is_not_for_public ? 'Not for Public' : 'Public' }}</p>@if($content->published_at)<p class="mt-2"><b>Published At:</b> {{ $content->published_at->format('d M Y H:i') }}</p>@endif</div></section>
            @endif
        </aside>
    </div>

    @if($canEdit)
    <div data-testid="edit-content-modal" x-cloak x-show="editOpen" @click.self="editOpen=false" class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><form method="post" action="{{ route('content.update',$content) }}" class="relative max-h-[92vh] w-full max-w-4xl overflow-y-auto rounded-2xl bg-white p-6 shadow-2xl">@csrf @method('PUT')<button type="button" @click="editOpen=false" class="absolute right-5 top-4" aria-label="Close edit content">✕</button><h2 class="text-lg font-bold">Edit Content</h2><p class="page-subtitle mb-4">Attribution and workflow status are protected system fields.</p><div class="grid gap-3 md:grid-cols-2"><label class="text-xs font-semibold md:col-span-2">Title<input data-testid="edit-title" class="field mt-1 w-full" name="title" value="{{ $content->title }}" required></label><label class="text-xs font-semibold">Publish Date<input class="field mt-1 w-full" type="date" name="publish_date" value="{{ $content->publish_date?->format('Y-m-d') }}"></label><label class="text-xs font-semibold">PIC<select class="field mt-1 w-full" name="pic_user_id"><option value="">No PIC</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected($content->pic_user_id===$user->id)>{{ $user->name }}</option>@endforeach</select></label><label class="text-xs font-semibold">Pillar<select data-testid="edit-pillar" x-model="pillar" class="field mt-1 w-full" name="pillar_id" required>@foreach($pillars as $pillar)<option value="{{ $pillar->id }}">{{ $pillar->name }}</option>@endforeach</select></label><label class="text-xs font-semibold">Series<select data-testid="edit-series" class="field mt-1 w-full" name="series_id" required><template x-for="item in seriesList()"><option :value="item.id" x-text="item.name" :selected="String(item.id)==='{{ $content->series_id }}'"></option></template></select></label><label class="text-xs font-semibold">Format<select class="field mt-1 w-full" name="format_id"><option value="">No Format</option>@foreach($formats as $format)<option value="{{ $format->id }}" @selected($content->format_id===$format->id)>{{ $format->name }}</option>@endforeach</select></label></div><div class="mt-4"><p class="text-xs font-bold">PLATFORMS & ACCOUNTS</p><div class="mt-2 flex flex-wrap gap-3">@foreach($platforms as $platform)<label class="text-xs"><input type="checkbox" name="platform_ids[]" value="{{ $platform->id }}" @checked($selectedPlatforms->contains((string)$platform->id)) @change="togglePlatform('{{ $platform->id }}')"> {{ $platform->name }}</label>@endforeach</div><div class="mt-3 grid gap-3 md:grid-cols-2">@foreach($platforms as $platform)<label x-show="selectedPlatforms.includes('{{ $platform->id }}')" class="text-xs font-semibold">{{ $platform->name }} Account<select class="field mt-1 w-full" name="platform_accounts[{{ $platform->id }}]" :disabled="!selectedPlatforms.includes('{{ $platform->id }}')" required><option value="">Select account</option>@foreach($platform->accounts as $account)<option value="{{ $account->id }}" @selected(($selectedAccounts[(string)$platform->id]??null)===(string)$account->id)>{{ $account->display_name }}</option>@endforeach</select></label>@endforeach</div></div><details open class="mt-4"><summary class="font-bold text-sm">Brief</summary><div class="mt-2 grid gap-2 md:grid-cols-2">@foreach(['hook'=>'Hook','angle'=>'Angle','key_message'=>'Key Message','cta'=>'CTA','notes'=>'Notes'] as $key=>$label)<textarea class="field" name="{{ $key }}" placeholder="{{ $label }}">{{ $content->brief?->$key }}</textarea>@endforeach</div></details><details open class="mt-4"><summary class="font-bold text-sm">Script / Copy</summary><div class="mt-2 grid gap-2 md:grid-cols-2">@foreach(['main_copy'=>'Main Script / Copy','reels_script'=>'Reels Script','carousel_copy'=>'Carousel Copy','caption'=>'Caption','threads_copy'=>'Threads Copy'] as $key=>$label)<textarea class="field h-24" name="{{ $key }}" placeholder="{{ $label }}">{{ $content->brief?->$key }}</textarea>@endforeach</div></details><details class="mt-4"><summary class="font-bold text-sm">Add Asset Reference</summary><div class="mt-2 grid gap-2 md:grid-cols-2"><input class="field" name="asset_name" placeholder="Asset Name"><select class="field" name="asset_type">@foreach(['Figma','Google Drive','Raw Footage','Reference','Image','Video','Thumbnail','Other'] as $type)<option>{{ $type }}</option>@endforeach</select><input class="field md:col-span-2" type="url" name="asset_url" placeholder="External URL"><textarea class="field md:col-span-2" name="asset_notes" placeholder="Notes"></textarea></div></details><div class="mt-5 flex justify-end gap-2"><button type="button" @click="editOpen=false" class="btn border">Cancel</button><button data-testid="save-content-edit" class="btn btn-blue">Save Changes</button></div></form></div>
    @endif

    @if($canManagePublishedInformation)
    <div data-testid="published-link-modal" x-cloak x-show="publishedOpen" @click.self="publishedOpen=false" class="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4"><form method="post" action="{{ route('content.published-information',$content) }}" class="relative w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl">@csrf @method('PUT')<button data-testid="published-modal-close" type="button" @click="publishedOpen=false" class="absolute right-5 top-4" aria-label="Close published information">✕</button><h2 class="text-lg font-bold">Add Published Link</h2><p class="page-subtitle mb-4">Record one global posting link for this content.</p><label class="text-xs font-semibold">Posting Link<input data-testid="published-url" class="field mt-1 w-full" type="url" name="final_url" value="{{ $content->final_url }}" placeholder="https://instagram.com/..."></label><div class="mt-5 flex flex-wrap justify-end gap-2"><button data-testid="not-for-public" class="btn border" name="visibility" value="not_for_public">Not for Public</button><button data-testid="save-published-url" class="btn bg-emerald-600 text-white" name="visibility" value="public">Submit</button></div></form></div>
    @endif
</div>
@endsection
