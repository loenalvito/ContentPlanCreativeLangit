@extends('layouts.app') @section('content')<h1 class="text-2xl font-bold">Team</h1><p class="text-xs text-slate-500 mb-5">Ringkasan workload tim Creative</p><div class="panel divide-y">@foreach($users as $u)<div class="p-4 flex flex-col md:flex-row md:items-center gap-4"><div class="w-10 h-10 rounded-full bg-blue-100 text-blue-600 grid place-items-center font-bold">{{ substr($u->name,0,1) }}</div><div class="w-48"><b class="text-sm">{{ $u->name }}</b><div class="text-xs text-slate-400">{{ $u->roles->first()?->name }}</div></div><div class="grid grid-cols-4 gap-5 text-xs flex-1"><span>Assigned <b>{{ $u->assigned_count }}</b></span><span>On Progress <b>{{ $u->progress_count }}</b></span><span>Review <b>{{ $u->review_count }}</b></span><span>Scheduled <b>{{ $u->scheduled_count }}</b></span></div></div>@endforeach</div>@endsection
