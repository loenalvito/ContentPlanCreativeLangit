@extends('layouts.app') @section('content')<h1 class="text-2xl font-bold">Published Library</h1><p class="text-xs text-slate-500 mb-5">Arsip semua konten yang sudah dipublish</p><div class="grid sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">@forelse($contents as $c)<article class="panel overflow-hidden"><div class="h-32 bg-gradient-to-br from-slate-800 to-blue-900 grid place-items-center text-white text-3xl">▶</div><div class="p-4"><a class="font-bold text-sm" href="{{ route('content.show',$c) }}">{{ $c->title }}</a><p class="text-xs text-slate-400 mt-2">{{ $c->publish_date?->format('d M Y') }} · {{ $c->pillar->name }}</p><p class="text-xs text-slate-400">{{ $c->platforms->pluck('name')->join(', ') }} · {{ $c->pic?->name }}</p>@if($c->final_url)<a class="text-xs text-blue-600 block mt-3" target="_blank" href="{{ $c->final_url }}">Open Post ↗</a>@endif</div></article>@empty<p class="text-slate-400">Belum ada published content.</p>@endforelse</div><div class="mt-4">{{ $contents->links() }}</div>@endsection
